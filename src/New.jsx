import React from 'react'

function New() {
  return (
    <div>
        <header style={{backgroundColor:"red"}}>
            <h1>React
The library for web and native user interfaces</h1>
  
        </header>
        <main>Add interactivity wherever you need it
React components receive data and return what should appear on the screen. You can pass them new data in response to an interaction, like when the user types into an input. React will then update the screen to match the new data.</main>
    </div>
  )
}

export default New
